{
  "name": "web",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "web/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "dev": {
      "executor": "nxVite.devServer",
      "options": {
        "buildTarget": "web:build",
        "port": 5173
      }
    },
    "serve": {
      "executor": "nxVite.devServer",
      "options": {
        "buildTarget": "web:build",
        "port": 5173
      }
    },
    "build": {
      "executor": "nxVite.build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/web",
        "configFile": "web/vite.config.ts"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "preview": {
      "executor": "nxVite.preview",
      "options": {
        "buildTarget": "web:build",
        "port": 4173
      }
    },
    "test": {
      "executor": "nxVite.test",
      "outputs": ["{workspaceRoot}/coverage/web"],
      "options": {
        "passWithNoTests": false,
        "reportsDirectory": "coverage/web"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["web/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p web/tsconfig.app.json"
      }
    }
  }
}
